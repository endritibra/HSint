<!DOCTYPE html>
<html lang="en">
<head>
    <title>Getting Started With Hubspot OAuth</title>
</head>
<body>
    <h1>Getting Started With HubSpot OAuth</h1>
    <% if (token) { %>
        <p>You're authenticated using the token: <%= token %></p>
        <p>Your account has these cool contacts:</p>
        <ul>
            <% contacts.forEach(contact => { %>
                <li><%= contact.properties.firstname.value %> <%= contact.properties.lastname.value %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>You're not authenticated</p>
        <p>Please install me <a href="<%= authUrl %>">here</a></p>
    <% } %>
</body>
</html>